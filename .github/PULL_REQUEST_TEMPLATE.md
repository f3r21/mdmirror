## Summary

<!-- 1-3 sentences on what changed and why -->

## Test plan

- [ ] `pytest -q -m "not integration"` passes
- [ ] `pytest -q` (incl. integration) passes
- [ ] `ruff check .` and `ruff format --check .` pass
- [ ] Manual smoke test if behavior changed

## Notes for reviewers

<!-- anything subtle, anything you're unsure about -->
